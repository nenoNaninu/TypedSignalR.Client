<#@ template language="C#" linePragmas="false" debug="false" hostspecific="false"#>
<#@ assembly name="System.Core" #>
<#@ import namespace="System.Linq" #>
<#@ import namespace="System.Text" #>
<#@ import namespace="System.Collections.Generic" #>
<#@ output extension=".cs" #>
// <auto-generated>
// THIS (.cs) FILE IS GENERATED BY TypedSignalR.Client
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using Microsoft.AspNetCore.SignalR.Client;

namespace TypedSignalR.Client
{
    public interface IHubConnectionObserver
    {
        Task OnClosed(Exception e);
        Task OnReconnected(string connectionId);
        Task OnReconnecting(Exception e);
    }

    public static partial class Extensions
    {
        private static class HubInvokerConstructorCache<T>
        {
#pragma warning disable CS0649
            public static Func<HubConnection, T> Construct;
#pragma warning restore CS0649 
        }

        private static class ReceiverBinderCache<T>
        {
#pragma warning disable CS0649
            public static Func<HubConnection, T, CompositeDisposable> Bind;
#pragma warning restore CS0649
        }

        public static THub CreateHubProxy<THub>(this HubConnection source)
        {
            return source.CreateHubProxyCore<THub>();
        }

        public static IDisposable Register<TReceiver>(this HubConnection source, TReceiver receiver)
        {
            return source.RegisterCore<TReceiver>(receiver);
        }

        public static (THub HubProxy, IDisposable Subscription) CreateHubProxyWith<THub, TReceiver>(this HubConnection source, TReceiver receiver)
        {
            var hubProxy = source.CreateHubProxyCore<THub>();
            var subscription = source.RegisterCore<TReceiver>(receiver);
            return (hubProxy, subscription);
        }

        private static THub CreateHubProxyCore<THub>(this HubConnection source)
        {
            return HubInvokerConstructorCache<THub>.Construct(source);
        }

        private static IDisposable RegisterCore<TReceiver>(this HubConnection source, TReceiver receiver)
        {
            if(typeof(TReceiver) == typeof(IHubConnectionObserver))
            {
                return new HubConnectionEventSubscription(source, receiver as IHubConnectionObserver);;
            }

            var compositeDisposable = ReceiverBinderCache<TReceiver>.Bind(source, receiver);

            if (receiver is IHubConnectionObserver hubConnectionEvent)
            {
                var evnetSubscription = new HubConnectionEventSubscription(source, hubConnectionEvent);
                compositeDisposable.Add(evnetSubscription);
            }

            return compositeDisposable;
        }

        private sealed class CompositeDisposable : IDisposable
        {
            private readonly object _gate = new();
            private readonly List<IDisposable> _disposables;

            private bool _disposed;

            public CompositeDisposable()
            {
                _disposables = new List<IDisposable>();
            }

            public CompositeDisposable(int capacity)
            {
                _disposables = new List<IDisposable>(capacity < 0 ? 0 : capacity);
            }

            public void Add(IDisposable item)
            {
                var shouldDispose = false;

                lock (_gate)
                {
                    shouldDispose = _disposed;

                    if (!_disposed)
                    {
                        _disposables.Add(item);
                    }
                }

                if (shouldDispose)
                {
                    item.Dispose();
                }
            }

            public void Dispose()
            {
                var currentDisposables = default(List<IDisposable>);

                lock (_gate)
                {
                    if (!_disposed)
                    {
                        _disposed = true;
                        currentDisposables = _disposables;
                    }
                }

                if (currentDisposables is not null)
                {
                    foreach (var item in currentDisposables)
                    {
                        if (item is not null)
                        {
                            item.Dispose();
                        }
                    }

                    currentDisposables.Clear();
                }
            }
        }

        private class HubConnectionEventSubscription : IDisposable
        {
            private readonly HubConnection _connection;
            private readonly IHubConnectionObserver _hubConnectionEvent;

            public HubConnectionEventSubscription(HubConnection connection, IHubConnectionObserver hubConnectionEvent)
            {
                _connection = connection;
                _hubConnectionEvent = hubConnectionEvent;
                _connection.Closed += _hubConnectionEvent.OnClosed;
                _connection.Reconnected += _hubConnectionEvent.OnReconnected;
                _connection.Reconnecting += _hubConnectionEvent.OnReconnecting;
            }

            public void Dispose()
            {
                _connection.Closed -= _hubConnectionEvent.OnClosed;
                _connection.Reconnected -= _hubConnectionEvent.OnReconnected;
                _connection.Reconnecting -= _hubConnectionEvent.OnReconnecting;
            }
        }
    }
}